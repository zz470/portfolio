---
phase: 11-dedicated-faq-page-inspired-by-defactosound-com-faq
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/copy.ts
  - app/faq/page.tsx
  - components/faq/FAQPageClient.tsx
autonomous: true
requirements:
  - FAQ-01
  - FAQ-02
  - FAQ-03
  - FAQ-04
  - FAQ-05
  - FAQ-06

must_haves:
  truths:
    - "Navigating to /faq shows a dedicated FAQ page with hero, 4 categorized accordion sections, and a bottom CTA"
    - "FAQ page contains 20+ Q&As distributed across 4 categories: Getting Started, Audio Post Process, Technical, About Me"
    - "Answers are detailed paragraphs with specific examples, platform/client name-drops, and SEO-rich language"
    - "FAQPage JSON-LD schema is present in the /faq page HTML for Google Rich Results"
  artifacts:
    - path: "lib/copy.ts"
      provides: "Categorized FAQ content with 20+ Q&As in copy.faqPage object"
      contains: "faqPage"
    - path: "app/faq/page.tsx"
      provides: "Server Component with metadata, JSON-LD, and FAQPageClient render"
      exports: ["default", "metadata"]
    - path: "components/faq/FAQPageClient.tsx"
      provides: "Client Component with hero, categorized accordions, CTA section"
      min_lines: 80
  key_links:
    - from: "app/faq/page.tsx"
      to: "lib/copy.ts"
      via: "import { copy }"
      pattern: "copy\\.faqPage"
    - from: "app/faq/page.tsx"
      to: "lib/jsonld.ts"
      via: "import { buildFAQSchema }"
      pattern: "buildFAQSchema"
    - from: "components/faq/FAQPageClient.tsx"
      to: "lib/copy.ts"
      via: "import { copy }"
      pattern: "copy\\.faqPage\\.categories"
---

<objective>
Create the dedicated /faq page with 20+ categorized Q&As, hero section, accordion UI, bottom CTA, and full SEO treatment.

Purpose: Establishes the FAQ page as the canonical FAQ destination with comprehensive, SEO-rich content inspired by defactosound.com/faq. Moves from 7 generic Q&As to 20+ detailed, categorized entries targeting long-tail sound design hiring queries.

Output: `/faq` route rendering a fully styled FAQ page with FAQPage JSON-LD structured data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dedicated-faq-page-inspired-by-defactosound-com-faq/11-CONTEXT.md

@lib/copy.ts
@app/services/page.tsx
@app/portfolio/category/[category]/page.tsx
@components/services/ServicesPageClient.tsx
@components/ui/accordion.tsx
@lib/jsonld.ts

<interfaces>
From lib/copy.ts (current FAQ structure to expand):
```typescript
faq: {
  sectionTitle: "Frequently Asked Questions",
  entries: [
    { question: string, answer: string },
    // ... 7 entries
  ],
}
```

From lib/jsonld.ts:
```typescript
export function buildFAQSchema(entries: readonly { question: string; answer: string }[]): object;
```

From components/ui/accordion.tsx:
```typescript
// Radix-based: AccordionItem, AccordionTrigger, AccordionContent
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@/components/ui/accordion";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add categorized FAQ content to lib/copy.ts</name>
  <files>lib/copy.ts</files>
  <read_first>
    - lib/copy.ts (full file — understand existing structure, voice rules in header comment, current faq.entries)
    - .planning/phases/11-dedicated-faq-page-inspired-by-defactosound-com-faq/11-CONTEXT.md (D-01 through D-05 for content decisions)
  </read_first>
  <action>
Add a new `faqPage` property to the `copy` object in lib/copy.ts (keep the existing `faq` property untouched for now — Plan 02 will handle the Expertise page teaser).

Structure the new `faqPage` as:

```typescript
faqPage: {
  meta: {
    title: "FAQ — Sound Design & Audio Post-Production",
    description: "Answers to common questions about sound design, mixing, audio post-production workflow, turnaround times, deliverables, and working with Lorenzo Pardell.",
  },
  hero: {
    title: "Frequently Asked Questions",
    subtitle: "Everything you need to know about working with me on your next project.",
  },
  categories: [
    {
      name: "Getting Started",
      entries: [ { question: string, answer: string }, ... ]
    },
    {
      name: "Audio Post-Production Process",
      entries: [ ... ]
    },
    {
      name: "Technical",
      entries: [ ... ]
    },
    {
      name: "About Me",
      entries: [ ... ]
    },
  ],
  cta: {
    title: "Still have questions?",
    description: "I'm happy to discuss your project and answer anything not covered here.",
    primaryAction: { label: "Get in Touch", href: "/contact" },
    secondaryAction: { label: "WhatsApp", href: "https://wa.me/message/INNHVBQDC3WKO1" },
  },
}
```

Per D-02, create 20+ total Q&As across the 4 categories (roughly 5-6 per category). Per D-03, answers must be Defacto-level depth — detailed paragraphs with specific examples, name-drops of platforms/clients (HBO, Netflix, Globoplay), technical specifics (5.1, 7.1, Dolby Atmos, Pro Tools, Source-Connect), and SEO-rich language targeting long-tail queries.

Per D-04, draft all content based on existing copy patterns, project data, and the voice rules at the top of lib/copy.ts (industry peer register, first person, no fluff, concrete numbers).

Redistribute the existing 7 Q&As from `copy.faq.entries` into the appropriate categories, expanding their answers. Add 13+ new Q&As covering topics like:
- Getting Started: how to reach out, what info to provide, typical project kickoff, pricing/quoting process, remote collaboration setup
- Audio Post Process: full pipeline explanation, dialogue editing specifics, Foley and ADR details, sound design approach, re-recording mix stages, deliverable formats
- Technical: DAW and tools used, file format requirements, delivery specs, remote session technology, quality control process
- About Me: background and experience, types of projects, location and availability, what sets you apart, languages spoken

WhatsApp href should use the same URL pattern from `lib/contact-info.ts`.
  </action>
  <verify>
    <automated>cd /Users/lorenzopardell/Desktop/DEV/NEXTJS/_PORTOFOLIO/next-portfolio && node -e "const {copy} = require('./lib/copy.ts'.replace('.ts','')); console.log('categories:', copy.faqPage.categories.length); const total = copy.faqPage.categories.reduce((s,c) => s + c.entries.length, 0); console.log('total QAs:', total); console.log('has meta:', !!copy.faqPage.meta.title); console.log('has hero:', !!copy.faqPage.hero.title); console.log('has cta:', !!copy.faqPage.cta.title);" 2>/dev/null || npx tsx -e "import {copy} from './lib/copy'; console.log('categories:', copy.faqPage.categories.length); const total = copy.faqPage.categories.reduce((s,c) => s + c.entries.length, 0); console.log('total QAs:', total);"</automated>
  </verify>
  <acceptance_criteria>
    - `copy.faqPage.categories` has exactly 4 entries
    - Total Q&A count across all categories is >= 20
    - `copy.faqPage.meta.title` contains "FAQ"
    - `copy.faqPage.hero.title` is "Frequently Asked Questions"
    - `copy.faqPage.cta.primaryAction.href` is "/contact"
    - `copy.faq.entries` (original) still exists unchanged (7 entries)
    - Category names are: "Getting Started", "Audio Post-Production Process", "Technical", "About Me"
    - Answers contain specific terms: "Pro Tools", "5.1", "Dolby Atmos" appear somewhere in the entries
  </acceptance_criteria>
  <done>lib/copy.ts contains a faqPage object with 4 categories, 20+ Q&As with detailed professional answers, hero content, meta content, and CTA content</done>
</task>

<task type="auto">
  <name>Task 2: Create /faq route (Server Component) and FAQPageClient</name>
  <files>app/faq/page.tsx, components/faq/FAQPageClient.tsx</files>
  <read_first>
    - app/portfolio/category/[category]/page.tsx (Server Component pattern with generateMetadata and JSON-LD)
    - components/portfolio/CategoryPageClient.tsx (Client Component page wrapper pattern)
    - components/services/ServicesPageClient.tsx (lines 340-375 — existing FAQ accordion styling)
    - components/ui/accordion.tsx (Radix accordion API)
    - lib/copy.ts (the faqPage object created in Task 1)
    - lib/jsonld.ts (buildFAQSchema function signature)
    - components/home/CTASection.tsx (CTA section pattern for potential reuse at bottom)
  </read_first>
  <action>
**Create `app/faq/page.tsx`** — Server Component following the pattern from `app/portfolio/category/[category]/page.tsx`:

```typescript
import type { Metadata } from "next";
import FAQPageClient from "@/components/faq/FAQPageClient";
import { copy } from "@/lib/copy";
import { buildFAQSchema } from "@/lib/jsonld";

export const metadata: Metadata = {
  title: copy.faqPage.meta.title,  // will be "FAQ — Sound Design & Audio Post-Production"
  description: copy.faqPage.meta.description,
  openGraph: {
    title: `${copy.faqPage.meta.title} | Lorenzo Pardell`,
    description: copy.faqPage.meta.description,
    images: ["/og-image.png"],
  },
  twitter: {
    card: "summary",
    title: `${copy.faqPage.meta.title} | Lorenzo Pardell`,
    description: copy.faqPage.meta.description,
    images: ["/og-image.png"],
  },
};

export default function FAQPage() {
  // Flatten all category entries for JSON-LD (buildFAQSchema expects flat array)
  const allEntries = copy.faqPage.categories.flatMap((cat) => cat.entries);
  const faqJsonLd = buildFAQSchema(allEntries);
  return (
    <>
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: JSON.stringify(faqJsonLd) }}
      />
      <FAQPageClient />
    </>
  );
}
```

Per D-13/D-14, this is the canonical FAQ page with FAQPage JSON-LD. Per D-11, the /services page JSON-LD removal happens in Plan 02.

**Create `components/faq/FAQPageClient.tsx`** — Client Component with "use client" directive:

1. **Hero section** (per D-06): Heading "Frequently Asked Questions" + subtitle from `copy.faqPage.hero`. Use the same container pattern `max-w-screen-xl mx-auto px-6 md:px-10`, gray-50 background section like other page heroes. Use `motion.div` with fadeIn animation.

2. **Category sections** (per D-07): Map over `copy.faqPage.categories`. Each category gets:
   - A section heading (`h2`) with the category name, styled like other section titles (text-2xl font-semibold text-gray-900)
   - Orange bar divider (`w-12 h-1 bg-orange-500 rounded-full`)
   - An `<Accordion type="single" collapsible>` (per D-08) containing that category's entries
   - AccordionTrigger: `text-left text-gray-800 font-medium text-base hover:text-orange-600` (matching existing FAQ styling from ServicesPageClient)
   - AccordionContent: `text-gray-600 leading-relaxed`
   - Each category section wrapped in `motion.div` with slideUp animation and staggered delay

3. **CTA section** (per D-09): At the bottom, render a CTA section using `copy.faqPage.cta`. Include title, description, and two buttons — primary (Link to /contact) styled as orange button, secondary (WhatsApp link) styled as outline. Use the same container width. Match the site's CTA patterns.

Import accordion from `@/components/ui/accordion`. Import `motion` from `framer-motion`. Import `copy` from `@/lib/copy`. Import `Link` from `next/link`.
  </action>
  <verify>
    <automated>cd /Users/lorenzopardell/Desktop/DEV/NEXTJS/_PORTOFOLIO/next-portfolio && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `app/faq/page.tsx` exists and does NOT contain "use client"
    - File `app/faq/page.tsx` contains `export const metadata: Metadata`
    - File `app/faq/page.tsx` contains `buildFAQSchema`
    - File `app/faq/page.tsx` contains `dangerouslySetInnerHTML`
    - File `components/faq/FAQPageClient.tsx` starts with `"use client"`
    - File `components/faq/FAQPageClient.tsx` contains `Accordion type="single" collapsible`
    - File `components/faq/FAQPageClient.tsx` contains `copy.faqPage.categories.map`
    - File `components/faq/FAQPageClient.tsx` contains `motion.div`
    - File `components/faq/FAQPageClient.tsx` contains `copy.faqPage.cta`
    - `npx next build` succeeds without errors
  </acceptance_criteria>
  <done>/faq route renders a page with hero section, 4 categorized accordion sections with all Q&As, bottom CTA, and FAQPage JSON-LD in the HTML</done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. `curl -s http://localhost:3000/faq | grep -o 'FAQPage'` returns a match (JSON-LD present)
3. The page renders hero, 4 category headings, accordion items, and CTA section
</verification>

<success_criteria>
- /faq page loads with hero, 4 categorized FAQ sections, and CTA
- 20+ Q&As visible across all categories
- FAQPage JSON-LD present in page source
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/11-dedicated-faq-page-inspired-by-defactosound-com-faq/11-01-SUMMARY.md`
</output>
