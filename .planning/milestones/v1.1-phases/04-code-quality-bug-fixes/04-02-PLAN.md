---
phase: 04-code-quality-bug-fixes
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - app/about/page.tsx
  - app/contact/page.tsx
  - components/home/ServicesSection.tsx
  - components/home/SelectedWorks.tsx
  - components/portfolio/PortfolioDetail.tsx
  - components/portfolio/PortfolioDetailSecondary.tsx
  - components/portfolio/PortfolioDetailTertiary.tsx
  - components/portfolio/ProjectNavigation.tsx
  - components/portfolio/ProjectVideoCard.tsx
  - components/portfolio/PortfolioGrid.tsx
  - components/ui/WhatsAppLive.tsx
  - components/navigation/footer.tsx
autonomous: false
requirements: [QUAL-01, QUAL-02, QUAL-03]

must_haves:
  truths:
    - "Every component using fadeIn/slideUp/staggerContainer imports from lib/animations.ts — no local definitions remain"
    - "Every file displaying phone/email/whatsapp imports from lib/contact-info.ts — no hardcoded contact strings remain"
    - "Every component sorting projects by date calls sortProjectsByDate — no inline .sort() on release_date remains"
    - "All pages render correctly with no visual regressions"
  artifacts:
    - path: "app/about/page.tsx"
      provides: "About page using shared animations and contact info"
      contains: "import { fadeIn, slideUp } from"
    - path: "components/portfolio/PortfolioGrid.tsx"
      provides: "Portfolio grid using shared sorting"
      contains: "import { sortProjectsByDate } from"
    - path: "app/contact/page.tsx"
      provides: "Contact page using shared contact info"
      contains: "import { contactInfo } from"
  key_links:
    - from: "components/portfolio/PortfolioGrid.tsx"
      to: "lib/data/projects.ts"
      via: "sortProjectsByDate import"
      pattern: "import.*sortProjectsByDate.*from.*@/lib/data/projects"
    - from: "app/contact/page.tsx"
      to: "lib/contact-info.ts"
      via: "contactInfo import"
      pattern: "import.*contactInfo.*from.*@/lib/contact-info"
    - from: "app/about/page.tsx"
      to: "lib/animations.ts"
      via: "animation variant import"
      pattern: "import.*fadeIn.*from.*@/lib/animations"
---

<objective>
Rewire all consumer components to import from the shared utility modules created in Plan 01, then verify the site visually.

Purpose: Complete the centralization by replacing every inline animation variant, hardcoded contact string, and inline sorting logic with imports from the shared modules.
Output: All consumers rewired, zero duplication, visual verification confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-code-quality-bug-fixes/04-CONTEXT.md
@.planning/phases/04-code-quality-bug-fixes/04-01-SUMMARY.md

<interfaces>
<!-- Created by Plan 01 — executor imports these directly -->

From lib/animations.ts:
```typescript
import type { Variants } from "framer-motion";
export const fadeIn: Variants = { initial: { opacity: 0, y: 20 }, animate: { opacity: 1, y: 0, transition: { duration: 0.6 } } };
export const slideUp: Variants = { initial: { opacity: 0, y: 30 }, animate: { opacity: 1, y: 0, transition: { duration: 0.5 } } };
export const staggerContainer: Variants = { initial: { opacity: 0 }, animate: { opacity: 1, transition: { staggerChildren: 0.1 } } };
```

From lib/contact-info.ts:
```typescript
export const contactInfo = {
  phone: "+5511917619699",
  email: "lorenzopardell@gmail.com",
  whatsappMessage: "Hello! I'm interested in your sound services.",
  phoneLink: "tel:+5511917619699",
  emailLink: "mailto:lorenzopardell@gmail.com",
  whatsappLink: "https://api.whatsapp.com/send?phone=...",
};
```

From lib/data/projects.ts:
```typescript
export function sortProjectsByDate(projectList: Project[]): Project[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire all consumers to shared modules</name>
  <files>app/about/page.tsx, app/contact/page.tsx, components/home/ServicesSection.tsx, components/home/SelectedWorks.tsx, components/portfolio/PortfolioDetail.tsx, components/portfolio/PortfolioDetailSecondary.tsx, components/portfolio/PortfolioDetailTertiary.tsx, components/portfolio/ProjectNavigation.tsx, components/portfolio/ProjectVideoCard.tsx, components/portfolio/PortfolioGrid.tsx, components/ui/WhatsAppLive.tsx, components/navigation/footer.tsx</files>
  <read_first>
    - lib/animations.ts (the shared module to import from)
    - lib/contact-info.ts (the shared module to import from)
    - lib/data/projects.ts (contains sortProjectsByDate to import)
    - app/about/page.tsx (has local fadeIn, slideUp definitions AND hardcoded email/phone)
    - app/contact/page.tsx (has hardcoded phoneNumber, whatsappLink, email, phone)
    - components/home/ServicesSection.tsx (has local fadeIn and staggerContainer)
    - components/home/SelectedWorks.tsx (has local fadeIn)
    - components/portfolio/PortfolioDetail.tsx (has local fadeIn)
    - components/portfolio/PortfolioDetailSecondary.tsx (has local fadeIn)
    - components/portfolio/PortfolioDetailTertiary.tsx (has local fadeIn)
    - components/portfolio/ProjectNavigation.tsx (has local fadeIn and inline sorting)
    - components/portfolio/ProjectVideoCard.tsx (has local fadeIn — was modified in Plan 01 for bug fix)
    - components/portfolio/PortfolioGrid.tsx (has inline sorting)
    - components/ui/WhatsAppLive.tsx (has hardcoded phoneNumber and whatsappLink)
    - components/navigation/footer.tsx (has hardcoded email and phone)
  </read_first>
  <action>
**QUAL-01: Replace all local animation variants with imports from `@/lib/animations`:**

For each file listed below, remove the local `const fadeIn = { ... }` (and `slideUp`/`staggerContainer` where present) and add `import { fadeIn } from "@/lib/animations";` (or `{ fadeIn, slideUp }`, `{ fadeIn, staggerContainer }` as needed).

IMPORTANT: Some files use `hidden/visible` keys (e.g., `app/page.tsx` homepage sections use `hidden`/`visible`). The shared module uses `initial/animate` keys. When replacing, also update the `motion.*` elements that reference `variants={fadeIn}` — if they use `initial="hidden" animate="visible"`, change to `initial="initial" animate="animate"`. Read each file carefully to check which key convention it uses.

Files and what to replace:

1. `app/about/page.tsx` — Remove local `const fadeIn` (lines ~9-12) and `const slideUp` (lines ~14-19). Add: `import { fadeIn, slideUp } from "@/lib/animations";`

2. `components/home/ServicesSection.tsx` — Remove local `const fadeIn` and any local `staggerContainer`. Add: `import { fadeIn, staggerContainer } from "@/lib/animations";`. Check if the file uses `hidden/visible` variants — if so, update motion elements to use `initial="initial" animate="animate"`.

3. `components/home/SelectedWorks.tsx` — Remove local `const fadeIn`. Add: `import { fadeIn } from "@/lib/animations";`. Check variant key convention and update if needed.

4. `components/portfolio/PortfolioDetail.tsx` — Remove local `const fadeIn`. Add: `import { fadeIn } from "@/lib/animations";`

5. `components/portfolio/PortfolioDetailSecondary.tsx` — Remove local `const fadeIn`. Add: `import { fadeIn } from "@/lib/animations";`

6. `components/portfolio/PortfolioDetailTertiary.tsx` — Remove local `const fadeIn`. Add: `import { fadeIn } from "@/lib/animations";`

7. `components/portfolio/ProjectNavigation.tsx` — Remove local `const fadeIn`. Add: `import { fadeIn } from "@/lib/animations";`. Note: this fadeIn has a `delay: 0.2` — the shared one does not. This is acceptable; the 0.2s delay was arbitrary and inconsistent with other components.

8. `components/portfolio/ProjectVideoCard.tsx` — Remove local `const fadeIn` (inside the component function). Add: `import { fadeIn } from "@/lib/animations";` at the top of the file.

**QUAL-03: Replace all hardcoded contact info with imports from `@/lib/contact-info`:**

1. `app/contact/page.tsx` — Remove local `const phoneNumber = "+5511917619699"` and `const whatsappLink = ...`. Add: `import { contactInfo } from "@/lib/contact-info";`. Replace:
   - `phoneNumber` references → `contactInfo.phone`
   - `whatsappLink` references → `contactInfo.whatsappLink`
   - `href="mailto:lorenzopardell@gmail.com"` → `href={contactInfo.emailLink}`
   - `lorenzopardell@gmail.com` text → `{contactInfo.email}`
   - `href="tel:+5511917619699"` → `href={contactInfo.phoneLink}`
   - Keep the local `message` const if it's the same as `contactInfo.whatsappMessage`, or remove it if redundant.

2. `components/ui/WhatsAppLive.tsx` — Remove local `const phoneNumber = "+5511917619699"` and `const whatsappLink = ...`. Add: `import { contactInfo } from "@/lib/contact-info";`. Replace `whatsappLink` references with `contactInfo.whatsappLink`. Also remove `const message` if it becomes unused.

3. `components/navigation/footer.tsx` — Add: `import { contactInfo } from "@/lib/contact-info";`. Replace:
   - `href="mailto:lorenzopardell@gmail.com"` → `href={contactInfo.emailLink}`
   - `lorenzopardell@gmail.com` text → `{contactInfo.email}`
   - `href="tel:+5511917619699"` → `href={contactInfo.phoneLink}`

4. `app/about/page.tsx` — Add: `import { contactInfo } from "@/lib/contact-info";`. Replace:
   - `href="mailto:lorenzopardell@gmail.com"` → `href={contactInfo.emailLink}`
   - `lorenzopardell@gmail.com` text → `{contactInfo.email}`
   - `href="tel:+5511917619699"` → `href={contactInfo.phoneLink}`

**QUAL-02: Replace inline sorting with `sortProjectsByDate`:**

1. `components/portfolio/PortfolioGrid.tsx` — Remove the inline `const sortedProjects = [...projects].sort(...)` block (lines 7-16). Add `sortProjectsByDate` to the existing import from `@/lib/data/projects`: `import { type Project, sortProjectsByDate } from "@/lib/data/projects";`. Replace with: `const sortedProjects = sortProjectsByDate(projects);`

2. `components/portfolio/ProjectNavigation.tsx` — Remove the inline `const sortedProjects = [...allProjects].sort(...)` block (lines 20-29). Add `sortProjectsByDate` to the existing import from `@/lib/data/projects`: `import { type Project, sortProjectsByDate } from "@/lib/data/projects";`. Replace with: `const sortedProjects = sortProjectsByDate(allProjects);`

After all changes, run `npx next build` to verify no import errors or type issues.
  </action>
  <verify>
    <automated>! grep -rn "const fadeIn\|const slideUp\|const staggerContainer" app/ components/ --include="*.tsx" --include="*.ts" | grep -v node_modules | grep -v ".claude/" && ! grep -rn "lorenzopardell@gmail.com\|5511917619699" app/ components/ --include="*.tsx" --include="*.ts" | grep -v node_modules | grep -v ".claude/" && ! grep -n "\.sort((a, b)" components/portfolio/PortfolioGrid.tsx components/portfolio/ProjectNavigation.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Zero files in app/ or components/ contain `const fadeIn =` or `const slideUp =` or `const staggerContainer =` (local definitions)
    - Zero files in app/ or components/ contain hardcoded string `lorenzopardell@gmail.com` or `5511917619699`
    - PortfolioGrid.tsx contains `import { type Project, sortProjectsByDate } from "@/lib/data/projects"` and `sortProjectsByDate(projects)` — no inline .sort()
    - ProjectNavigation.tsx contains `import { type Project, sortProjectsByDate } from "@/lib/data/projects"` and `sortProjectsByDate(allProjects)` — no inline .sort()
    - All 8 animation consumer files contain `import { fadeIn` or `import { fadeIn, slideUp` or `import { fadeIn, staggerContainer` from `"@/lib/animations"`
    - All 4 contact info consumer files contain `import { contactInfo } from "@/lib/contact-info"`
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>All animation, contact, and sorting consumers rewired to shared modules. Zero duplication remains. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all pages</name>
  <files>none</files>
  <action>
Start the dev server and present the verification checklist to the user. The user visually inspects all pages to confirm no regressions from the refactoring. The CTA section should now have a dark/black background (the one intentional visual change from the bug fix in Plan 01).
  </action>
  <what-built>
    Centralized animations, contact info, and sorting across the entire site. Fixed CTA section background, ProjectVideoCard iframe error handling, and removed dead code. All changes are refactoring — no intentional visual changes except the CTA section which should now show a dark background instead of white.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Visit http://localhost:3000 — Home page: check hero, services section, selected works, and CTA section. **CTA section should have a dark/black background with amber/orange text** (previously was white).
    3. Visit http://localhost:3000/portfolio — Portfolio grid should show projects sorted newest first.
    4. Click any project to view detail page — animations should work (fade in on load).
    5. Visit http://localhost:3000/about — Check email and phone links work, animations work.
    6. Visit http://localhost:3000/contact — Check email, phone, WhatsApp links work.
    7. Visit http://localhost:3000/services — Check animations work.
    8. Open browser console on each page — should see zero console.log or console.warn output.
    9. Check the WhatsApp floating button (bottom-right corner) — should still link correctly.
  </how-to-verify>
  <verify>User visually confirms all pages render correctly</verify>
  <done>User approves visual state of all pages with no regressions</done>
  <resume-signal>Type "approved" or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
- `grep -rn "const fadeIn\|const slideUp\|const staggerContainer" app/ components/ --include="*.tsx" | grep -v ".claude/"` returns no matches
- `grep -rn "lorenzopardell@gmail.com\|5511917619699" app/ components/ --include="*.tsx" | grep -v ".claude/"` returns no matches
- `grep -rn "\.sort((a, b)" components/portfolio/PortfolioGrid.tsx components/portfolio/ProjectNavigation.tsx` returns no matches
- `grep -c "import.*from.*@/lib/animations" app/about/page.tsx components/home/ServicesSection.tsx components/home/SelectedWorks.tsx components/portfolio/PortfolioDetail.tsx components/portfolio/PortfolioDetailSecondary.tsx components/portfolio/PortfolioDetailTertiary.tsx components/portfolio/ProjectNavigation.tsx components/portfolio/ProjectVideoCard.tsx` — each returns 1
- `grep -c "import.*contactInfo.*from.*@/lib/contact-info" app/contact/page.tsx app/about/page.tsx components/ui/WhatsAppLive.tsx components/navigation/footer.tsx` — each returns 1
- Human confirms all pages render correctly
</verification>

<success_criteria>
- All animation variants imported from lib/animations.ts — no local definitions anywhere
- All contact info imported from lib/contact-info.ts — no hardcoded strings anywhere
- All project sorting uses sortProjectsByDate — no inline .sort() on release_date
- Human approves visual verification on all pages
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-quality-bug-fixes/04-02-SUMMARY.md`
</output>
