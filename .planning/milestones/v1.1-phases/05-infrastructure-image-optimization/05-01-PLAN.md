---
phase: 05-infrastructure-image-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/portfolio/PortfolioDetailSecondary.tsx
  - components/portfolio/ProjectNavigation.tsx
  - components/home/partner-logos.tsx
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "All project images render via next/image — zero raw <img> tags in project-related components"
    - "Partner logo images include sizes prop — no console warning about missing sizes"
    - "next.config.ts has no deprecated images.domains — INFRA-02 pre-satisfied, no changes needed"
  artifacts:
    - path: "components/portfolio/PortfolioDetailSecondary.tsx"
      provides: "Hero image via next/image with fill mode"
      contains: "import Image from"
    - path: "components/portfolio/ProjectNavigation.tsx"
      provides: "Navigation thumbnails via next/image with fill mode"
      contains: "import Image from"
    - path: "components/home/partner-logos.tsx"
      provides: "Partner logos with sizes prop"
      contains: "sizes="
  key_links:
    - from: "components/portfolio/PortfolioDetailSecondary.tsx"
      to: "next/image"
      via: "Image component with fill prop"
      pattern: "<Image[\\s\\S]*?fill"
    - from: "components/portfolio/ProjectNavigation.tsx"
      to: "next/image"
      via: "Image component with fill prop"
      pattern: "<Image[\\s\\S]*?fill"
---

<objective>
Replace the 3 remaining raw `<img>` tags with `next/image` and add missing `sizes` prop to partner logos.

Purpose: Satisfy INFRA-01 (all project images use next/image) and close the console warning from partner-logos. INFRA-02 is already satisfied (no deprecated config exists).
Output: Three updated component files with optimized images.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-infrastructure-image-optimization/05-CONTEXT.md

@components/portfolio/PortfolioDetailSecondary.tsx
@components/portfolio/ProjectNavigation.tsx
@components/home/partner-logos.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace raw img tags with next/image in PortfolioDetailSecondary and ProjectNavigation</name>
  <files>components/portfolio/PortfolioDetailSecondary.tsx, components/portfolio/ProjectNavigation.tsx</files>
  <read_first>
    - components/portfolio/PortfolioDetailSecondary.tsx
    - components/portfolio/ProjectNavigation.tsx
    - components/home/SelectedWorks.tsx (reference — existing fill pattern to follow)
  </read_first>
  <action>
    **PortfolioDetailSecondary.tsx** (per D-01, D-02, D-03):
    1. Add `import Image from "next/image"` at the top
    2. Replace the `<img>` tag on line 38-42 with:
       ```tsx
       <Image
         src={project.hero_url || project.thumbnail_url}
         alt={project.title}
         fill
         sizes="100vw"
         className="object-cover object-center"
       />
       ```
    3. The parent `div` already has `absolute inset-0` — the `fill` mode Image needs the parent to have `position: relative`. The grandparent div (`relative w-full h-[38vh]...`) already provides this. Remove the `<div className="absolute inset-0 bg-gray-100 dark:bg-gray-800">` wrapper and move its background to the grandparent, or keep the wrapper but ensure Image is positioned correctly within it. The simplest approach: keep the wrapper div (it provides the bg fallback), Image with `fill` will position relative to the nearest positioned ancestor (the grandparent with `relative`). Remove `w-full h-full` from className since `fill` handles sizing.
    4. Add `priority` prop since this is the hero image (above the fold). Per Claude's discretion on priority attribute.

    **ProjectNavigation.tsx** (per D-01, D-02, D-03):
    1. Add `import Image from "next/image"` at the top
    2. Replace the `<img>` tag on line 49-52 (previous project) with:
       ```tsx
       <Image
         src={prevProject.thumbnail_url}
         alt={prevProject.title}
         fill
         sizes="(max-width: 768px) 100vw, 50vw"
         className="object-cover object-center group-hover:scale-105 transition-transform duration-500"
       />
       ```
    3. Replace the `<img>` tag on line 72-75 (next project) with the same pattern using `nextProject`.
    4. Both parent divs already have `relative` and fixed height (`h-60`) — `fill` mode works directly. Remove `absolute inset-0 w-full h-full` from className since `fill` handles all positioning.
  </action>
  <verify>
    <automated>cd /Users/lorenzopardell/Desktop/DEV/NEXTJS/_PORTOFOLIO/next-portfolio && ! grep -n '<img' components/portfolio/PortfolioDetailSecondary.tsx components/portfolio/ProjectNavigation.tsx; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep "<img" components/portfolio/PortfolioDetailSecondary.tsx returns no matches
    - grep "<img" components/portfolio/ProjectNavigation.tsx returns no matches
    - grep "import Image from" components/portfolio/PortfolioDetailSecondary.tsx returns a match
    - grep "import Image from" components/portfolio/ProjectNavigation.tsx returns a match
    - grep 'sizes=' components/portfolio/PortfolioDetailSecondary.tsx returns a match
    - grep 'sizes=' components/portfolio/ProjectNavigation.tsx returns a match
    - grep 'fill' components/portfolio/PortfolioDetailSecondary.tsx returns a match
    - grep 'fill' components/portfolio/ProjectNavigation.tsx returns a match
  </acceptance_criteria>
  <done>Zero raw img tags remain in PortfolioDetailSecondary.tsx and ProjectNavigation.tsx. All 3 images use next/image with fill mode and appropriate sizes props.</done>
</task>

<task type="auto">
  <name>Task 2: Add sizes prop to partner-logos and verify build</name>
  <files>components/home/partner-logos.tsx</files>
  <read_first>
    - components/home/partner-logos.tsx
  </read_first>
  <action>
    **partner-logos.tsx** (per D-05):
    1. Add `sizes="150px"` prop to the `<Image>` component on line 70-74. The Image already uses `fill` mode inside a container with `width: '160px'` and `height: '60px'`. Adding `sizes="150px"` tells the browser the rendered size, eliminating the console warning.

    Result should be:
    ```tsx
    <Image
      src={partner.image}
      alt={`${partner.name} logo`}
      fill
      sizes="150px"
      className="object-contain"
    />
    ```

    **Build verification:**
    2. Run `npx next build` to confirm:
       - No deprecation warnings about `images.domains` (INFRA-02 pre-satisfied)
       - No TypeScript errors from the image changes
       - Build completes successfully
  </action>
  <verify>
    <automated>cd /Users/lorenzopardell/Desktop/DEV/NEXTJS/_PORTOFOLIO/next-portfolio && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep 'sizes=' components/home/partner-logos.tsx returns a match
    - grep "<img" components/portfolio/PortfolioDetailSecondary.tsx components/portfolio/ProjectNavigation.tsx returns no matches
    - npx next build completes without errors
  </acceptance_criteria>
  <done>Partner logos have sizes prop (no console warning). Build passes with zero deprecation warnings and zero TypeScript errors. INFRA-01 and INFRA-02 both satisfied.</done>
</task>

</tasks>

<verification>
1. `grep -rn "<img" components/portfolio/ components/home/` — zero raw img tags in project-related or partner components
2. `grep -rn "import Image" components/portfolio/PortfolioDetailSecondary.tsx components/portfolio/ProjectNavigation.tsx` — both files import next/image
3. `grep -rn "sizes=" components/portfolio/PortfolioDetailSecondary.tsx components/portfolio/ProjectNavigation.tsx components/home/partner-logos.tsx` — all three files have sizes prop
4. `npx next build` — completes successfully, no deprecation warnings
</verification>

<success_criteria>
- All 3 raw `<img>` tags replaced with `<Image>` from next/image using fill mode
- All image components have appropriate `sizes` prop (100vw for hero, responsive for nav thumbnails, 150px for logos)
- `next build` passes with zero errors and zero deprecation warnings
- INFRA-01: satisfied (all project images use next/image)
- INFRA-02: pre-satisfied (no deprecated config exists, confirmed by clean build)
</success_criteria>

<output>
After completion, create `.planning/phases/05-infrastructure-image-optimization/05-01-SUMMARY.md`
</output>
