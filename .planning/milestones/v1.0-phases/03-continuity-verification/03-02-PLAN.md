---
phase: 03-continuity-verification
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: []
autonomous: false
requirements: [CONT-01, CONT-02, CONT-03]

must_haves:
  truths:
    - "User visually confirms homepage renders hero, services, featured works"
    - "User visually confirms portfolio grid displays project cards with category filter"
    - "User visually confirms at least one secondary and the tertiary detail layout render differently from primary"
    - "User visually confirms prev/next navigation links work on a detail page"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all routes and interactive features in the browser to confirm visual correctness beyond what automated data checks can prove.

Purpose: Automated tests in Plan 01 verify data integrity and routing logic. This plan verifies the actual rendered UI — layout variants look different, filtering updates the grid, navigation links work visually.
Output: User confirmation that all routes and features work correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-continuity-verification/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server for manual verification</name>
  <files><!-- none: server start only --></files>
  <read_first>
    - app/layout.tsx (root layout structure)
  </read_first>
  <action>
Start the Next.js dev server with `npm run dev` (or `npx next dev --turbopack`) in the background. Wait for the "Ready" message. Print the local URL (typically http://localhost:3000) for the user.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:3000</automated>
  </verify>
  <acceptance_criteria>
    - Dev server is running and responding
    - `curl http://localhost:3000` returns HTTP 200
  </acceptance_criteria>
  <done>Dev server running and accessible at localhost:3000.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all routes and features</name>
  <what-built>Complete local data migration — all pages now use local TypeScript data instead of Supabase. Automated tests (Plan 01) confirmed data integrity. Now verifying visual correctness.</what-built>
  <how-to-verify>
    **Step 1 — Homepage (CONT-01):** Visit http://localhost:3000
    - Confirm hero section renders with placeholder image
    - Confirm services section displays
    - Confirm 3 featured project cards appear in "Selected Works"
    - Confirm footer renders

    **Step 2 — Portfolio grid + filtering (CONT-01, CONT-02):** Visit http://localhost:3000/portfolio
    - Confirm project cards appear in a grid
    - Click "Series" category filter — confirm only Series projects show
    - Click "Film" category filter — confirm only Film projects show (2 films)
    - Click "All" (or deselect) — confirm all 19 projects return

    **Step 3 — Detail page primary layout (CONT-01, CONT-03):** Visit http://localhost:3000/portfolio/operacao-transplante
    - Confirm project title, description, roles, and video embed display
    - Confirm prev/next navigation cards appear at bottom

    **Step 4 — Detail page secondary layout (CONT-03):** Visit http://localhost:3000/portfolio/bimbo
    - Confirm this page has a visually different layout than the primary one above

    **Step 5 — Detail page tertiary layout (CONT-03):** Visit http://localhost:3000/portfolio/gnosis-vitamin-k2-womens-health
    - Confirm this page has a visually different layout than both primary and secondary

    **Step 6 — Static pages (CONT-01):** Visit each:
    - http://localhost:3000/services — services list renders
    - http://localhost:3000/about — about content renders
    - http://localhost:3000/contact — contact info renders

    **Step 7 — Prev/next navigation (CONT-04):** On any detail page, click "Next Project" and "Previous Project" links.
    - Confirm they navigate to different projects
    - Confirm no broken links or errors
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
User has visually confirmed all 6 routes render, category filtering works, all 3 design versions display, and prev/next navigation functions.
</verification>

<success_criteria>
User types "approved" confirming all visual checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-continuity-verification/03-02-SUMMARY.md`
</output>
