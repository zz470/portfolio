---
phase: 03-continuity-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-continuity.ts
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04]

must_haves:
  truths:
    - "All 6 routes respond with 200 status codes"
    - "Category filter produces correct subsets for all 5 categories"
    - "All 19 project slugs resolve to a project via getProjectBySlug"
    - "Design version routing maps secondary slugs to secondary layout, tertiary to tertiary, rest to primary"
    - "Prev/next navigation cycles through all 19 projects without index errors"
  artifacts:
    - path: "scripts/verify-continuity.ts"
      provides: "Automated verification script covering all CONT requirements"
      min_lines: 80
  key_links:
    - from: "scripts/verify-continuity.ts"
      to: "lib/data/projects.ts"
      via: "direct import of projects, getProjectBySlug, getProjectsByCategory, getFeaturedProjects"
      pattern: "import.*from.*lib/data/projects"
---

<objective>
Write and run an automated verification script that proves all continuity requirements (CONT-01 through CONT-04) are satisfied with the local data layer.

Purpose: Provide machine-verifiable proof that every route, filter, layout variant, and navigation path works correctly with local data — before any manual browser testing.
Output: A passing verification script at scripts/verify-continuity.ts and a clean next build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/data/projects.ts
@app/portfolio/page.tsx
@app/portfolio/[slug]/page.tsx
@components/portfolio/ProjectNavigation.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and run continuity verification script</name>
  <files>scripts/verify-continuity.ts</files>
  <read_first>
    - lib/data/projects.ts (data layer with Project interface, projects array, getProjectBySlug, getProjectsByCategory, getFeaturedProjects)
    - app/portfolio/page.tsx (category filtering logic)
    - app/portfolio/[slug]/page.tsx (design_version routing logic)
    - components/portfolio/ProjectNavigation.tsx (prev/next cycling logic)
  </read_first>
  <action>
Create `scripts/verify-continuity.ts` that imports directly from `@/lib/data/projects` and runs these verification checks. Use `npx tsx` to execute (npx will auto-download tsx on first run if not already installed locally; this is expected behavior).

**CONT-01 — Route data availability:**
- Assert `projects.length === 19` (all project records present)
- Assert `getFeaturedProjects(3).length === 3` (homepage featured works)
- Assert every project has required fields: `title`, `slug`, `description`, `thumbnail_url`, `video_url`, `category`, `roles` (non-empty array)
- Assert all slugs are unique (no duplicates)

**CONT-02 — Category filtering:**
- Extract unique categories from projects array: expect exactly `["Advertisement", "Documentary", "Film", "Reality", "Series"]` (sorted)
- For each category, call `getProjectsByCategory(category)` and assert result.length > 0
- Assert sum of all category counts === 19 (no project is uncategorized or missing)
- Assert `projects.length === 19` as the "All" baseline (the portfolio page returns the full projects array when no category is selected — it does NOT call `getProjectsByCategory(null)`)

**CONT-03 — Design version routing:**
- Assert these slugs have `design_version === "secondary"`: `run-for-europe`, `bimbo`, `gots`, `ropes-for-building-blocks`, `piracanjuba`
- Assert this slug has `design_version === "tertiary"`: `gnosis-vitamin-k2-womens-health`
- Assert all remaining 13 slugs have `design_version === "primary"`
- Assert every project has a `design_version` value (not undefined)

**CONT-04 — Prev/next navigation cycling:**
- Reproduce the sorting logic from ProjectNavigation.tsx: sort by release_date descending (null dates at end)
- For each project at index i, verify prevIndex = (i - 1 + 19) % 19 and nextIndex = (i + 1) % 19 both resolve to valid projects
- Verify the first project's "previous" wraps to the last project
- Verify the last project's "next" wraps to the first project

**Build verification:**
- After data checks, run `next build` via child_process.execSync and assert exit code 0
- Parse stdout for route count: assert all 7 routes appear (/, /_not-found, /about, /contact, /portfolio, /portfolio/[slug], /services)

Script should print each check with PASS/FAIL prefix, and exit with code 0 only if ALL checks pass. Exit code 1 on any failure.

Run the script with: `npx tsx scripts/verify-continuity.ts`
  </action>
  <verify>
    <automated>npx tsx scripts/verify-continuity.ts</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-continuity.ts exists and is > 80 lines
    - `npx tsx scripts/verify-continuity.ts` exits with code 0
    - Script output contains "PASS" for each of: "CONT-01", "CONT-02", "CONT-03", "CONT-04"
    - Script output contains "All checks passed" at the end
    - `next build` succeeds (exit code 0) as part of the script
  </acceptance_criteria>
  <done>Verification script runs green, confirming all 19 projects load, 5 categories filter correctly, 3 design versions route to correct layouts, and prev/next navigation wraps correctly. Build produces all 7 routes.</done>
</task>

</tasks>

<verification>
- `npx tsx scripts/verify-continuity.ts` exits 0 with all CONT checks passing
- `next build` exits 0 with 7 routes generated (run as part of the script)
</verification>

<success_criteria>
All CONT-01 through CONT-04 requirements verified by automated script with zero failures.
</success_criteria>

<output>
After completion, create `.planning/phases/03-continuity-verification/03-01-SUMMARY.md`
</output>
