---
phase: 02-supabase-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - app/portfolio/page.tsx
  - app/portfolio/[slug]/page.tsx
  - components/home/SelectedWorks.tsx
  - components/home/HeroSection.tsx
  - components/portfolio/PortfolioCard.tsx
  - components/portfolio/PortfolioGrid.tsx
  - components/portfolio/PortfolioDetail.tsx
  - components/portfolio/PortfolioDetailSecondary.tsx
  - components/portfolio/PortfolioDetailTertiary.tsx
  - components/portfolio/ProjectDetailsCard.tsx
  - components/portfolio/ProjectAboutCard.tsx
  - components/portfolio/ProjectVideoCard.tsx
  - components/portfolio/ProjectNavigation.tsx
autonomous: true
requirements: [SUPA-02]

must_haves:
  truths:
    - "All pages import data from @/lib/data/projects instead of hooks"
    - "No file in the project imports from @/hooks/useProjects or @/hooks/useProject"
    - "Homepage renders featured projects without loading state"
    - "Portfolio page renders project grid without loading/error state from Supabase"
    - "Project detail page renders correct design variant without loading state"
    - "HeroSection uses a local placeholder image instead of Supabase storage URL"
  artifacts:
    - path: "app/page.tsx"
      provides: "Homepage with direct data import"
      contains: "import { getFeaturedProjects }"
    - path: "app/portfolio/page.tsx"
      provides: "Portfolio page with direct data import"
      contains: "import { projects }"
    - path: "app/portfolio/[slug]/page.tsx"
      provides: "Project detail page with direct data import"
      contains: "import { getProjectBySlug, projects }"
  key_links:
    - from: "app/page.tsx"
      to: "lib/data/projects.ts"
      via: "import { getFeaturedProjects }"
      pattern: "from \"@/lib/data/projects\""
    - from: "app/portfolio/page.tsx"
      to: "lib/data/projects.ts"
      via: "import { projects }"
      pattern: "from \"@/lib/data/projects\""
    - from: "app/portfolio/[slug]/page.tsx"
      to: "lib/data/projects.ts"
      via: "import { getProjectBySlug, projects }"
      pattern: "from \"@/lib/data/projects\""
---

<objective>
Rewire all page and component imports from Supabase hooks to local data file.

Purpose: Replace every consumer of useProjects/useProject hooks and the Project type with direct imports from `@/lib/data/projects`. Remove async loading/error state patterns that only existed for Supabase fetching. Per D-01, D-03, D-08.
Output: All 14 consumer files updated to use local data imports. No file references `@/hooks/useProjects` or `@/hooks/useProject`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-supabase-removal/02-CONTEXT.md

@lib/data/projects.ts
</context>

<interfaces>
<!-- Key exports from the local data file that all consumers will import from -->

From lib/data/projects.ts:
```typescript
export interface Project {
  id: number;
  title: string;
  slug: string;
  description: string;
  thumbnail_url: string;
  video_url: string;
  category: string;
  roles: string[];
  imdb_url?: string;
  production_company?: string;
  media_platform?: string;
  release_date?: number;
  hero_url?: string;
  design_version?: string;
}

export const projects: Project[] = [ /* 19 records */ ];

export function getProjectBySlug(slug: string): Project | undefined;
export function getProjectsByCategory(category: string): Project[];
export function getFeaturedProjects(count: number = 3): Project[];
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewire page-level imports and simplify data flow</name>
  <files>app/page.tsx, app/portfolio/page.tsx, app/portfolio/[slug]/page.tsx</files>
  <read_first>
    - app/page.tsx (current homepage using useProjects hook)
    - app/portfolio/page.tsx (current portfolio page using useProjects hook)
    - app/portfolio/[slug]/page.tsx (current detail page using useProject + useProjects hooks)
    - lib/data/projects.ts (source of truth for Project type and helper functions)
  </read_first>
  <action>
    **app/page.tsx** (per D-01, D-03):
    - Remove `import { useProjects } from "@/hooks/useProjects"`
    - Add `import { getFeaturedProjects } from "@/lib/data/projects"`
    - Remove `const { projects, loading } = useProjects();`
    - Replace the `featuredProjects` computation (lines 53-63) with `const featuredProjects = getFeaturedProjects(3);` — the data file already returns projects sorted by release_date desc
    - Remove `isLoading={loading}` prop from `<SelectedWorks>`, pass `isLoading={false}` instead (keep prop to avoid breaking SelectedWorks interface)
    - Keep `"use client"` directive (per D-02)
    - Keep all other imports, useEffect for intersection observer, animation variants unchanged

    **app/portfolio/page.tsx** (per D-01, D-03):
    - Remove `import { useProjects } from "@/hooks/useProjects"`
    - Add `import { projects } from "@/lib/data/projects"`
    - Remove `const { projects, loading, error } = useProjects();`
    - Remove the entire error return block (lines 35-44: the `if (error) return (...)` block)
    - In `categories` useMemo: change guard from `if (!projects) return []` to just compute directly since `projects` is always a defined array
    - In `filteredProjects` useMemo: change guard from `if (!projects) return []` to `if (selectedCategory === null) return projects;` (remove the null check since projects is always defined)
    - Keep `"use client"` directive, keep all motion wrappers, keep CategoryFilter and PortfolioGrid

    **app/portfolio/[slug]/page.tsx** (per D-01, D-03):
    - Remove `import { useProject } from "@/hooks/useProject"`
    - Remove `import { useProjects } from "@/hooks/useProjects"`
    - Add `import { getProjectBySlug, projects } from "@/lib/data/projects"`
    - Remove `const { project, loading: projectLoading, error: projectError } = useProject(slug as string);`
    - Remove `const { projects, loading: projectsLoading, error: projectsError } = useProjects();`
    - Remove `const loading = projectLoading || projectsLoading;` and `const error = projectError || projectsError;`
    - Add `const project = getProjectBySlug(slug as string);`
    - Remove the entire loading block (lines 29-44: `if (loading) { ... }`)
    - Change error block (line 46): remove `error ||` from condition, keep `if (!project)` guard with the "Project Not Found" card UI
    - Remove all `console.log` statements (6 of them)
    - Update design version renders: change `projects || []` to just `projects` on all three render calls (PortfolioDetail, PortfolioDetailSecondary, PortfolioDetailTertiary)
    - Keep `"use client"` directive, keep useParams, keep all other imports
  </action>
  <verify>
    <automated>cd /Users/lorenzopardell/Desktop/DEV/NEXTJS/_PORTOFOLIO/next-portfolio && grep -r "from \"@/hooks/useProjects\"\|from \"@/hooks/useProject\"" app/ && echo "FAIL: hook imports still present in app/" || echo "PASS: no hook imports in app/"</automated>
  </verify>
  <acceptance_criteria>
    - app/page.tsx contains `import { getFeaturedProjects } from "@/lib/data/projects"`
    - app/page.tsx does NOT contain `useProjects`
    - app/page.tsx contains `const featuredProjects = getFeaturedProjects(3)`
    - app/portfolio/page.tsx contains `import { projects } from "@/lib/data/projects"`
    - app/portfolio/page.tsx does NOT contain `useProjects` or `error` state
    - app/portfolio/[slug]/page.tsx contains `import { getProjectBySlug, projects } from "@/lib/data/projects"`
    - app/portfolio/[slug]/page.tsx does NOT contain `useProject` or `useProjects`
    - app/portfolio/[slug]/page.tsx does NOT contain `console.log`
    - app/portfolio/[slug]/page.tsx does NOT contain `loading` variable
    - All three files still contain `"use client"` directive
  </acceptance_criteria>
  <done>All three page files import from @/lib/data/projects. No hook references remain in app/ directory. Loading/error states from Supabase are removed.</done>
</task>

<task type="auto">
  <name>Task 2: Update component imports and fix HeroSection Supabase URL</name>
  <files>components/home/SelectedWorks.tsx, components/home/HeroSection.tsx, components/portfolio/PortfolioCard.tsx, components/portfolio/PortfolioGrid.tsx, components/portfolio/PortfolioDetail.tsx, components/portfolio/PortfolioDetailSecondary.tsx, components/portfolio/PortfolioDetailTertiary.tsx, components/portfolio/ProjectDetailsCard.tsx, components/portfolio/ProjectAboutCard.tsx, components/portfolio/ProjectVideoCard.tsx, components/portfolio/ProjectNavigation.tsx</files>
  <read_first>
    - components/portfolio/PortfolioCard.tsx
    - components/portfolio/PortfolioGrid.tsx
    - components/portfolio/PortfolioDetail.tsx
    - components/portfolio/PortfolioDetailSecondary.tsx
    - components/portfolio/PortfolioDetailTertiary.tsx
    - components/portfolio/ProjectDetailsCard.tsx
    - components/portfolio/ProjectAboutCard.tsx
    - components/portfolio/ProjectVideoCard.tsx
    - components/portfolio/ProjectNavigation.tsx
    - components/home/SelectedWorks.tsx
    - components/home/HeroSection.tsx
  </read_first>
  <action>
    **All 9 portfolio components** (per D-08):
    Each of these files has `import { Project } from "@/hooks/useProjects"` — change every one to `import { type Project } from "@/lib/data/projects"`:
    - components/portfolio/PortfolioCard.tsx
    - components/portfolio/PortfolioGrid.tsx
    - components/portfolio/PortfolioDetail.tsx
    - components/portfolio/PortfolioDetailSecondary.tsx
    - components/portfolio/PortfolioDetailTertiary.tsx
    - components/portfolio/ProjectDetailsCard.tsx
    - components/portfolio/ProjectAboutCard.tsx
    - components/portfolio/ProjectVideoCard.tsx
    - components/portfolio/ProjectNavigation.tsx

    **components/home/SelectedWorks.tsx** (per D-08):
    - Change `import { Project } from "@/hooks/useProjects"` to `import { type Project } from "@/lib/data/projects"`

    **components/home/HeroSection.tsx** (Supabase storage URL removal):
    - Replace the default value for `backgroundImageUrl` prop from the long Supabase storage signed URL (`https://crxuhqqswealopkmdpxq.supabase.co/storage/v1/...`) to `/images/hero-placeholder.svg`
    - Create a simple hero placeholder SVG at `public/images/hero-placeholder.svg`: a 1920x1080 dark gray rectangle with "Hero Image" text centered, matching the style of existing project placeholders

    No other changes to any component's logic, props, or JSX — only import paths and the HeroSection default URL.
  </action>
  <verify>
    <automated>cd /Users/lorenzopardell/Desktop/DEV/NEXTJS/_PORTOFOLIO/next-portfolio && grep -r "from \"@/hooks/useProjects\"\|from \"@/hooks/useProject\"\|supabase.co" components/ && echo "FAIL: old imports still present in components/" || echo "PASS: no old imports in components/"</automated>
  </verify>
  <acceptance_criteria>
    - Zero files in components/ contain `from "@/hooks/useProjects"`
    - Zero files in components/ contain `supabase.co`
    - All 10 component files contain `from "@/lib/data/projects"`
    - components/home/HeroSection.tsx contains `backgroundImageUrl = "/images/hero-placeholder.svg"`
    - public/images/hero-placeholder.svg exists and is a valid SVG file
    - No component logic, props, or JSX has been changed beyond the import line and HeroSection default URL
  </acceptance_criteria>
  <done>All 11 component files import Project type from @/lib/data/projects. HeroSection uses local placeholder image. No reference to Supabase remains in components/.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -r "useProjects\|useProject\|@/hooks\|supabase" --include="*.tsx" --include="*.ts" app/ components/ lib/data/` returns zero matches (excluding lib/data itself which is the source)
2. All imports from `@/lib/data/projects` resolve correctly (TypeScript compilation check in Plan 02)
</verification>

<success_criteria>
- Zero references to `@/hooks/useProjects` or `@/hooks/useProject` in the entire codebase outside the hook files themselves
- Zero references to `supabase.co` in any component file
- All 3 page files use direct data imports
- All 11 component files use `@/lib/data/projects` for the Project type
- HeroSection default image points to a local file
</success_criteria>

<output>
After completion, create `.planning/phases/02-supabase-removal/02-01-SUMMARY.md`
</output>
