---
phase: 05-infrastructure-image-optimization
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - components/portfolio/PortfolioDetail.tsx
  - components/portfolio/PortfolioCard.tsx
autonomous: true
gap_closure: true
requirements:
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "Zero raw <img> tags exist in any portfolio component — grep '<img' returns no matches across all components/portfolio/ files"
    - "All project images render via next/image with fill mode and sizes prop"
    - "PortfolioCard preserves onLoad/onError callback behavior for loading states"
  artifacts:
    - path: "components/portfolio/PortfolioDetail.tsx"
      provides: "Hero images (mobile + desktop) via next/image"
      contains: "import Image from \"next/image\""
    - path: "components/portfolio/PortfolioCard.tsx"
      provides: "Thumbnail image via next/image with onLoad/onError"
      contains: "import Image from \"next/image\""
  key_links:
    - from: "components/portfolio/PortfolioDetail.tsx"
      to: "next/image"
      via: "<Image fill> with sizes prop"
      pattern: "<Image[\\s\\S]*?fill"
    - from: "components/portfolio/PortfolioCard.tsx"
      to: "next/image"
      via: "<Image fill> with onLoad and onError callbacks"
      pattern: "<Image[\\s\\S]*?fill[\\s\\S]*?onLoad"
---

<objective>
Close verification gap: replace 3 remaining raw `<img>` tags with `next/image` in PortfolioDetail.tsx and PortfolioCard.tsx.

Purpose: Complete INFRA-01 requirement — all project images must use next/image for automatic optimization, lazy loading, and responsive sizing.
Output: Two updated component files with zero raw `<img>` tags remaining in any portfolio component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-infrastructure-image-optimization/05-VERIFICATION.md
@.planning/phases/05-infrastructure-image-optimization/05-01-SUMMARY.md

<interfaces>
<!-- Established next/image pattern from PortfolioDetailSecondary.tsx (done in 05-01): -->
From components/portfolio/PortfolioDetailSecondary.tsx:
```tsx
import Image from "next/image";
// ...
<Image
  src={project.hero_url || project.thumbnail_url}
  alt={project.title}
  fill
  sizes="100vw"
  className="object-cover"
  priority
/>
```

<!-- AspectRatio component from shadcn/ui: -->
From components/ui/aspect-ratio.tsx:
```tsx
// Renders a div with position: relative and paddingBottom based on ratio
// Children with position: absolute + inset: 0 fill the aspect ratio box
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace raw img tags in PortfolioDetail.tsx with next/image</name>
  <files>components/portfolio/PortfolioDetail.tsx</files>
  <read_first>
    - components/portfolio/PortfolioDetail.tsx
    - components/portfolio/PortfolioDetailSecondary.tsx (reference pattern for fill mode)
    - components/ui/aspect-ratio.tsx (understand container behavior)
  </read_first>
  <action>
1. Add `import Image from "next/image";` after the existing imports (after line 13, before the interface).

2. Replace the mobile hero image at line 66 (inside AspectRatio ratio={2/3}):
   - Remove: `<img src={project.hero_url || project.thumbnail_url} alt={project.title} className="w-full h-full object-cover" />`
   - Replace with: `<Image src={project.hero_url || project.thumbnail_url} alt={project.title} fill sizes="(max-width: 1024px) 100vw, 40vw" className="object-cover" priority />`
   - Note: AspectRatio renders a relative-positioned container, so `fill` mode works directly inside it. The `w-full h-full` classes are not needed with fill mode.

3. Replace the desktop hero image at line 94 (inside div.w-full.relative):
   - Remove: `<img src={project.hero_url || project.thumbnail_url} alt={project.title} className="absolute inset-0 w-full h-full object-cover" />`
   - Replace with: `<Image src={project.hero_url || project.thumbnail_url} alt={project.title} fill sizes="100vw" className="object-cover" priority />`
   - Note: Parent div already has `relative` class. The `absolute inset-0 w-full h-full` classes are handled by next/image's fill mode automatically.

Per D-01 and D-02 from CONTEXT.md: use fill mode since containers already use absolute inset-0 + object-cover pattern.
  </action>
  <verify>
    <automated>grep -n "&lt;img" /Users/lorenzopardell/Desktop/DEV/NEXTJS/_PORTOFOLIO/next-portfolio/components/portfolio/PortfolioDetail.tsx; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '<img' components/portfolio/PortfolioDetail.tsx` returns zero matches
    - `grep 'import Image from "next/image"' components/portfolio/PortfolioDetail.tsx` returns exactly 1 match
    - `grep -c '<Image' components/portfolio/PortfolioDetail.tsx` returns 2 (mobile + desktop)
    - `grep 'fill' components/portfolio/PortfolioDetail.tsx` returns matches for both Image components
    - `grep 'sizes=' components/portfolio/PortfolioDetail.tsx` returns matches for both Image components
    - `grep 'priority' components/portfolio/PortfolioDetail.tsx` returns matches (hero images are above the fold)
  </acceptance_criteria>
  <done>PortfolioDetail.tsx has zero raw img tags; both mobile and desktop hero images render via next/image with fill mode, sizes prop, and priority attribute</done>
</task>

<task type="auto">
  <name>Task 2: Replace raw img tag in PortfolioCard.tsx with next/image</name>
  <files>components/portfolio/PortfolioCard.tsx</files>
  <read_first>
    - components/portfolio/PortfolioCard.tsx
    - components/portfolio/PortfolioDetailSecondary.tsx (reference pattern for fill mode)
  </read_first>
  <action>
1. Add `import Image from "next/image";` after the existing `import Link from "next/link";` on line 1.

2. Replace the thumbnail image at line 59 (inside div.relative.w-full.pb-[50%]):
   - Remove: `<img src={project.thumbnail_url} alt={project.title} className={`absolute inset-0 w-full h-full object-cover transition-opacity duration-300 ${imageLoaded ? 'opacity-100' : 'opacity-0'}`} onLoad={() => setImageLoaded(true)} onError={() => setImageError(true)} />`
   - Replace with: `<Image src={project.thumbnail_url} alt={project.title} fill sizes="(max-width: 768px) 100vw, 50vw" className={`object-cover transition-opacity duration-300 ${imageLoaded ? 'opacity-100' : 'opacity-0'}`} onLoad={() => setImageLoaded(true)} onError={() => setImageError(true)} />`
   - Note: Parent div `relative w-full pb-[50%]` provides the positioned ancestor. The `absolute inset-0 w-full h-full` classes are handled by next/image's fill mode. The `onLoad` and `onError` callbacks are supported directly by next/image's Image component.
   - The `sizes` prop uses `(max-width: 768px) 100vw, 50vw` because the portfolio grid is single column on mobile and 2-column on desktop (matching the ProjectNavigation pattern from 05-01).
  </action>
  <verify>
    <automated>grep -n "&lt;img" /Users/lorenzopardell/Desktop/DEV/NEXTJS/_PORTOFOLIO/next-portfolio/components/portfolio/PortfolioCard.tsx; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '<img' components/portfolio/PortfolioCard.tsx` returns zero matches
    - `grep 'import Image from "next/image"' components/portfolio/PortfolioCard.tsx` returns exactly 1 match
    - `grep -c '<Image' components/portfolio/PortfolioCard.tsx` returns 1
    - `grep 'fill' components/portfolio/PortfolioCard.tsx` returns a match on the Image component
    - `grep 'sizes=' components/portfolio/PortfolioCard.tsx` returns a match
    - `grep 'onLoad' components/portfolio/PortfolioCard.tsx` returns a match (callback preserved)
    - `grep 'onError' components/portfolio/PortfolioCard.tsx` returns a match (callback preserved)
  </acceptance_criteria>
  <done>PortfolioCard.tsx has zero raw img tags; thumbnail renders via next/image with fill mode, sizes prop, and preserved onLoad/onError callbacks for loading state management</done>
</task>

</tasks>

<verification>
After both tasks complete, run these checks to confirm the gap is fully closed:

1. `grep -r '<img' components/portfolio/` — must return zero matches across ALL portfolio components
2. `npx tsc --noEmit` — TypeScript compiles with zero errors
3. `npm run build` — Next.js build succeeds with no warnings about deprecated image config
</verification>

<success_criteria>
- Zero raw `<img>` tags in any file under `components/portfolio/`
- Both PortfolioDetail.tsx and PortfolioCard.tsx import and use `next/image` with `fill` mode
- All `<Image>` components have a `sizes` prop (no console warnings)
- PortfolioCard.tsx preserves onLoad/onError callbacks for loading/error state UI
- TypeScript compiles without errors
- `next build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-infrastructure-image-optimization/05-02-SUMMARY.md`
</output>
