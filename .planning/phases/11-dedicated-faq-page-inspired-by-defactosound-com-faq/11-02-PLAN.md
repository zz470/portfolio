---
phase: 11-dedicated-faq-page-inspired-by-defactosound-com-faq
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - components/services/ServicesPageClient.tsx
  - app/services/page.tsx
  - lib/copy.ts
  - components/navigation/header.tsx
  - components/navigation/footer.tsx
  - app/sitemap.ts
autonomous: false
requirements:
  - FAQ-07
  - FAQ-08
  - FAQ-09
  - FAQ-10

must_haves:
  truths:
    - "Expertise page shows a teaser with 2-3 top FAQs and a 'See all FAQs' link to /faq instead of the full FAQ section"
    - "FAQPage JSON-LD is only on /faq, not on /services"
    - "FAQ link appears in both header and footer navigation"
    - "/faq is included in the sitemap"
  artifacts:
    - path: "components/services/ServicesPageClient.tsx"
      provides: "FAQ teaser section replacing full FAQ accordion"
      contains: "See all FAQs"
    - path: "app/services/page.tsx"
      provides: "Server Component without FAQPage JSON-LD"
    - path: "app/sitemap.ts"
      provides: "Sitemap with /faq route"
      contains: "/faq"
    - path: "components/navigation/header.tsx"
      provides: "Header with FAQ nav item"
    - path: "components/navigation/footer.tsx"
      provides: "Footer with FAQ nav item"
  key_links:
    - from: "components/services/ServicesPageClient.tsx"
      to: "/faq"
      via: "Link component"
      pattern: "href.*faq"
    - from: "components/navigation/header.tsx"
      to: "/faq"
      via: "navItems in copy.ts"
      pattern: "faq"
    - from: "app/sitemap.ts"
      to: "/faq"
      via: "staticRoutes array"
      pattern: "faq"
---

<objective>
Integrate the FAQ page into the site: replace the full FAQ section on the Expertise page with a teaser, migrate JSON-LD to /faq only, add FAQ to navigation and sitemap.

Purpose: Ensures the FAQ page is discoverable through all navigation paths and search engines, while the Expertise page retains some FAQ value via a teaser without duplicating content.

Output: Updated Expertise page, navigation, and sitemap all pointing to /faq as the canonical FAQ destination.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dedicated-faq-page-inspired-by-defactosound-com-faq/11-CONTEXT.md
@.planning/phases/11-dedicated-faq-page-inspired-by-defactosound-com-faq/11-01-SUMMARY.md

@lib/copy.ts
@app/services/page.tsx
@components/services/ServicesPageClient.tsx
@components/navigation/header.tsx
@components/navigation/footer.tsx
@app/sitemap.ts

<interfaces>
From lib/copy.ts (after Plan 01):
```typescript
copy.faqPage.categories  // Array of { name, entries[] }
copy.faq.entries          // Original 7 entries (still present)
copy.header.navItems      // Array of { name, path }
copy.footer.navItems      // Array of { name, path }
```

From app/services/page.tsx (current — to be modified):
```typescript
import { buildFAQSchema } from "@/lib/jsonld";
// JSON-LD script block in JSX — to be removed
```

From app/sitemap.ts:
```typescript
const staticRoutes: MetadataRoute.Sitemap = [
  // ... existing routes — /faq to be added here
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Expertise page FAQ section with teaser + migrate JSON-LD + update nav/sitemap</name>
  <files>components/services/ServicesPageClient.tsx, app/services/page.tsx, lib/copy.ts, components/navigation/header.tsx, components/navigation/footer.tsx, app/sitemap.ts</files>
  <read_first>
    - components/services/ServicesPageClient.tsx (full file — understand FAQ section at lines 347-372 to replace with teaser)
    - app/services/page.tsx (full file — see JSON-LD injection to remove, lines 20-28)
    - lib/copy.ts (full file — see header.navItems and footer.navItems arrays, and existing faq object)
    - components/navigation/header.tsx (full file — understand navItems rendering)
    - components/navigation/footer.tsx (full file — understand navItems rendering)
    - app/sitemap.ts (full file — see staticRoutes array)
  </read_first>
  <action>
**6 files to modify — all are small, targeted changes:**

**1. `lib/copy.ts`** — Per D-10, add a `faqTeaser` property to the copy object (alongside existing `faq` and `faqPage`):

```typescript
faqTeaser: {
  sectionTitle: "Common Questions",
  linkText: "See all FAQs",
  linkHref: "/faq",
},
```

Also update `header.navItems` to add FAQ after About:
```typescript
{ name: "FAQ", path: "/faq" },
```

Also update `footer.navItems` to add FAQ after Contact:
```typescript
{ name: "FAQ", path: "/faq" },
```

Per D-12, FAQ must be linked from header, footer, and the Expertise page teaser.

**2. `components/services/ServicesPageClient.tsx`** — Per D-10, replace the full FAQ section (lines 347-372) with a teaser:

Replace the current `{/* FAQ Section */}` block with a teaser that:
- Shows `copy.faqTeaser.sectionTitle` as the heading
- Orange bar divider (same pattern)
- Shows only the first 3 entries from `copy.faq.entries` in a single-collapsible Accordion (reuse existing accordion pattern)
- Below the accordion, add a Link to `/faq` with text from `copy.faqTeaser.linkText` styled as: `inline-flex items-center text-orange-500 hover:text-orange-600 font-medium mt-4` with an arrow icon (use `→` or import ArrowRight from lucide-react)

Keep the same `motion.div` wrapper, same animation, same `styles.sectionContainer` and `styles.sectionContent` classes.

**3. `app/services/page.tsx`** — Per D-11, remove the FAQPage JSON-LD:
- Remove `import { buildFAQSchema } from "@/lib/jsonld";`
- Remove the `const faqJsonLd = buildFAQSchema(copy.faq.entries);` line
- Remove the entire `<script type="application/ld+json" ...>` block
- The component should just return `<ServicesPageClient />` directly (no fragment wrapper needed)

**4. `app/sitemap.ts`** — Per D-15, add `/faq` to the staticRoutes array:
```typescript
{
  url: `${baseUrl}/faq`,
  lastModified: new Date(),
  changeFrequency: "monthly",
  priority: 0.7,
},
```
Add it after the `/services` entry.

**5. `components/navigation/header.tsx`** — No code change needed (it reads from `copy.header.navItems` which was updated in lib/copy.ts).

**6. `components/navigation/footer.tsx`** — No code change needed (it reads from `copy.footer.navItems` which was updated in lib/copy.ts).
  </action>
  <verify>
    <automated>cd /Users/lorenzopardell/Desktop/DEV/NEXTJS/_PORTOFOLIO/next-portfolio && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/page.tsx` does NOT contain `buildFAQSchema`
    - `app/services/page.tsx` does NOT contain `application/ld+json`
    - `components/services/ServicesPageClient.tsx` contains `copy.faqTeaser.sectionTitle`
    - `components/services/ServicesPageClient.tsx` contains `copy.faq.entries.slice(0, 3)` or similar 3-entry limit
    - `components/services/ServicesPageClient.tsx` contains link to `/faq`
    - `components/services/ServicesPageClient.tsx` contains `See all FAQs` or `copy.faqTeaser.linkText`
    - `lib/copy.ts` contains `{ name: "FAQ", path: "/faq" }` in both header and footer navItems
    - `lib/copy.ts` contains `faqTeaser` property
    - `app/sitemap.ts` contains `/faq`
    - `npx next build` succeeds without errors
  </acceptance_criteria>
  <done>Expertise page shows FAQ teaser with 3 entries + link to /faq. JSON-LD removed from /services. FAQ appears in header and footer nav. /faq in sitemap.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify FAQ page and integration</name>
  <files>none</files>
  <what-built>
    Complete dedicated FAQ page at /faq with 20+ categorized Q&As, hero section, accordion UI, CTA section, and FAQPage JSON-LD. Expertise page now shows a 3-question teaser with link to full FAQ. Navigation updated with FAQ links in header and footer.
  </what-built>
  <how-to-verify>
    1. Visit http://localhost:3000/faq — verify:
       - Hero section with "Frequently Asked Questions" heading and subtitle
       - 4 category sections with headings: Getting Started, Audio Post-Production Process, Technical, About Me
       - Accordion opens one answer at a time, closes when another opens
       - Bottom CTA section with "Get in Touch" and "WhatsApp" buttons
       - Page styling consistent with rest of site (orange accents, Geist font, container width)
    2. View page source on /faq — verify FAQPage JSON-LD script tag present
    3. Visit http://localhost:3000/services — verify:
       - FAQ section now shows only 3 questions with a "See all FAQs" link
       - Clicking "See all FAQs" navigates to /faq
       - No JSON-LD script tag in page source
    4. Check header navigation — "FAQ" link visible and navigates to /faq
    5. Check footer navigation — "FAQ" link visible and navigates to /faq
    6. Review FAQ content quality — answers should be detailed, professional, and mention specific tools/clients/deliverables
  </how-to-verify>
  <action>Human verifies the FAQ page visually, checks navigation links, reviews content quality, and confirms JSON-LD presence in page source.</action>
  <verify>
    <automated>echo "checkpoint — manual verification required"</automated>
  </verify>
  <done>User confirms FAQ page content, layout, navigation integration, and SEO markup are all correct</done>
  <resume-signal>Type "approved" or describe issues with content or layout</resume-signal>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. /faq page renders with all 4 categories and 20+ Q&As
3. /services page no longer has FAQPage JSON-LD
4. Header and footer show FAQ navigation link
5. Sitemap includes /faq
</verification>

<success_criteria>
- /faq is the canonical FAQ page with full content and JSON-LD
- /services shows teaser with 3 FAQs + link to /faq
- FAQ discoverable via header, footer, and sitemap
- User approves content quality and layout
</success_criteria>

<output>
After completion, create `.planning/phases/11-dedicated-faq-page-inspired-by-defactosound-com-faq/11-02-SUMMARY.md`
</output>
