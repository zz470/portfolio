---
phase: 06-server-components-seo-error-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/sitemap.ts
  - app/robots.ts
  - app/not-found.tsx
  - app/error.tsx
  - public/og-image.png
autonomous: true
requirements: [SEO-02, SEO-03, SEO-04, ERR-01, ERR-02]

must_haves:
  truths:
    - "Visiting /sitemap.xml returns valid XML listing all static routes and all 19 project pages"
    - "Visiting /robots.txt returns a robots file allowing all crawlers with sitemap reference"
    - "Root layout metadata includes metadataBase, title template, and openGraph with OG image"
    - "Visiting a non-existent URL shows a branded 404 page with orange accent and navigation home"
    - "A runtime error shows a recovery UI with retry and navigate-home options"
  artifacts:
    - path: "app/sitemap.ts"
      provides: "Dynamic sitemap with static routes + project pages"
      exports: ["default"]
    - path: "app/robots.ts"
      provides: "Robots.txt allowing all crawlers"
      exports: ["default"]
    - path: "app/not-found.tsx"
      provides: "Branded 404 page"
      exports: ["default"]
    - path: "app/error.tsx"
      provides: "Global error boundary with retry"
      exports: ["default"]
    - path: "public/og-image.png"
      provides: "Static OG image placeholder"
    - path: "app/layout.tsx"
      provides: "Root metadata with metadataBase, title template, openGraph"
  key_links:
    - from: "app/sitemap.ts"
      to: "lib/data/projects.ts"
      via: "import projects"
      pattern: "import.*projects.*from.*@/lib/data/projects"
    - from: "app/layout.tsx"
      to: "public/og-image.png"
      via: "openGraph.images"
      pattern: "images.*og-image\\.png"
---

<objective>
Create the SEO infrastructure (sitemap, robots.txt, OG image, root metadata template) and error handling pages (404, error boundary).

Purpose: Establishes the foundation all other plans depend on -- the metadata template in root layout that child pages will extend, plus the error pages and SEO files.
Output: 6 files -- sitemap.ts, robots.ts, og-image.png, not-found.tsx, error.tsx, updated layout.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-server-components-seo-error-pages/06-CONTEXT.md
@.planning/phases/06-server-components-seo-error-pages/06-RESEARCH.md

@app/layout.tsx
@lib/data/projects.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SEO files and update root layout metadata</name>
  <files>app/sitemap.ts, app/robots.ts, public/og-image.png, app/layout.tsx</files>
  <read_first>
    - app/layout.tsx (current metadata export to preserve and extend)
    - lib/data/projects.ts (projects array for sitemap generation -- need slug fields)
  </read_first>
  <action>
    **1. Create `app/sitemap.ts`** (per D-12):
    - Import `MetadataRoute` from `next` and `projects` from `@/lib/data/projects`
    - Set `const baseUrl = "https://lorenzopardell.com"`
    - Export default function returning `MetadataRoute.Sitemap` array
    - Static routes: `/` (priority 1, monthly), `/portfolio` (priority 0.9, monthly), `/services` (priority 0.8, yearly), `/about` (priority 0.7, yearly), `/contact` (priority 0.6, yearly)
    - Dynamic routes: map over `projects` array to create `/portfolio/${p.slug}` entries (priority 0.8, yearly)
    - All entries use `lastModified: new Date()`

    **2. Create `app/robots.ts`** (per D-13):
    - Import `MetadataRoute` from `next`
    - Export default function returning `MetadataRoute.Robots`
    - Rules: `{ userAgent: "*", allow: "/" }`
    - Sitemap: `"https://lorenzopardell.com/sitemap.xml"`

    **3. Create `public/og-image.png`** (per D-04, D-05):
    - OG image must be PNG format (SVG not supported by social platforms per research)
    - Create a 1200x630 PNG placeholder using a Node.js script:
      - Dark background (#1a1a1a)
      - Orange-500 accent (#f97316) horizontal line
      - White text: "Lorenzo Pardell" (large) and "Sound Designer" (smaller)
    - If canvas libraries are not available, create a minimal valid PNG file as placeholder (even a solid dark background is fine -- user will replace with real image per D-04)
    - File must exist at `public/og-image.png`

    **4. Update `app/layout.tsx` metadata** (per D-09, D-10):
    - Replace the existing `metadata` export with:
    ```typescript
    export const metadata: Metadata = {
      metadataBase: new URL("https://lorenzopardell.com"),
      title: {
        template: "%s | Lorenzo Pardell",
        default: "Lorenzo Pardell | Sound Designer",
      },
      description: "Sound designer specializing in film, series, and documentaries based in Sao Paulo, Brazil. Delivering high-quality audio solutions for creative projects worldwide.",
      keywords: [/* keep all existing keywords unchanged */],
      robots: {
        index: true,
        follow: true,
      },
      icons: {
        icon: "/favicon.ico",
      },
      alternates: {
        canonical: "https://lorenzopardell.com",
      },
      openGraph: {
        siteName: "Lorenzo Pardell",
        images: [{ url: "/og-image.png", width: 1200, height: 630 }],
        locale: "en_US",
        type: "website",
      },
    };
    ```
    - Keep ALL existing keywords array values unchanged
    - Keep ALL other parts of layout.tsx unchanged (fonts, body, Header, Footer, etc.)
    - The `title.template` pattern means child pages set `title: "About"` and get "About | Lorenzo Pardell"
  </action>
  <verify>
    <automated>cd /Users/lorenzopardell/Desktop/DEV/NEXTJS/_PORTOFOLIO/next-portfolio && test -f app/sitemap.ts && test -f app/robots.ts && test -f public/og-image.png && grep -q "metadataBase" app/layout.tsx && grep -q "template.*Lorenzo Pardell" app/layout.tsx && grep -q "og-image.png" app/layout.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `app/sitemap.ts` exists and contains `MetadataRoute.Sitemap` return type
    - `app/sitemap.ts` contains `import { projects }` from `@/lib/data/projects`
    - `app/sitemap.ts` contains all 5 static routes: `/`, `/portfolio`, `/services`, `/about`, `/contact`
    - `app/sitemap.ts` maps `projects` to `/portfolio/${p.slug}` entries
    - `app/robots.ts` exists and contains `userAgent: "*"` and `allow: "/"`
    - `app/robots.ts` contains `sitemap: "https://lorenzopardell.com/sitemap.xml"`
    - `public/og-image.png` exists and is a valid PNG file
    - `app/layout.tsx` contains `metadataBase: new URL("https://lorenzopardell.com")`
    - `app/layout.tsx` contains `template: "%s | Lorenzo Pardell"`
    - `app/layout.tsx` contains `openGraph` with `images` referencing `/og-image.png`
    - `app/layout.tsx` contains `locale: "en_US"` (per D-10)
    - All existing keywords in `app/layout.tsx` are preserved
  </acceptance_criteria>
  <done>sitemap.ts generates routes for all static pages + 19 projects, robots.ts allows all crawlers, OG image placeholder exists, root layout has metadata template with openGraph</done>
</task>

<task type="auto">
  <name>Task 2: Create error handling pages</name>
  <files>app/not-found.tsx, app/error.tsx</files>
  <read_first>
    - app/layout.tsx (confirm root layout wraps not-found automatically)
    - components/ui/button.tsx (reuse Button component for CTAs)
  </read_first>
  <action>
    **1. Create `app/not-found.tsx`** (per D-07, ERR-01):
    - This is a Server Component -- NO `"use client"` directive
    - Import `Link` from `next/link` and `Button` from `@/components/ui/button`
    - Render a centered layout matching portfolio aesthetic:
      - Container: `flex flex-col items-center justify-center min-h-[60vh] px-6`
      - Large "404" heading: `text-6xl font-bold text-orange-500 mb-4`
      - "Page Not Found" subheading: `text-2xl font-bold mb-4`
      - Descriptive text: "The page you're looking for doesn't exist or has been moved." with `text-gray-600 dark:text-gray-400 mb-8 text-center max-w-md`
      - Button using `<Button asChild><Link href="/">Return Home</Link></Button>`
    - Root layout automatically wraps this with Header/Footer -- no extra layout needed

    **2. Create `app/error.tsx`** (per D-08, ERR-02):
    - MUST have `"use client"` as the first line (Next.js requirement for error boundaries)
    - Import `Button` from `@/components/ui/button`
    - Export default function with props: `{ error: Error & { digest?: string }; reset: () => void }`
    - NOTE: Use `reset` prop (not `unstable_retry`) -- `reset` is the stable API in Next.js 15.2.2
    - Render a centered recovery UI:
      - Container: `flex flex-col items-center justify-center min-h-[60vh] px-6`
      - Heading: "Something went wrong" with `text-2xl font-bold mb-4`
      - Description: "An unexpected error occurred. You can try again or return to the homepage." with `text-gray-600 dark:text-gray-400 mb-8 text-center max-w-md`
      - Two buttons in a `flex gap-4` row:
        - "Try Again" button: `<Button onClick={() => reset()}>Try Again</Button>`
        - "Return Home" button: `<Button variant="outline" asChild><a href="/">Return Home</a></Button>` (use `<a>` not `<Link>` for full page reload to clear error state)
    - Style consistently with not-found page (orange accent via Button default style)
  </action>
  <verify>
    <automated>cd /Users/lorenzopardell/Desktop/DEV/NEXTJS/_PORTOFOLIO/next-portfolio && test -f app/not-found.tsx && test -f app/error.tsx && grep -q "use client" app/error.tsx && ! grep -q "use client" app/not-found.tsx && grep -q "404" app/not-found.tsx && grep -q "reset" app/error.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `app/not-found.tsx` exists and does NOT contain `"use client"`
    - `app/not-found.tsx` contains `text-orange-500` for the 404 heading
    - `app/not-found.tsx` contains `Return Home` link pointing to `/`
    - `app/not-found.tsx` imports `Button` from `@/components/ui/button`
    - `app/error.tsx` exists and has `"use client"` as the first line
    - `app/error.tsx` accepts `reset` function prop and calls it on button click
    - `app/error.tsx` contains "Try Again" and "Return Home" options
    - `app/error.tsx` uses `<a href="/">` (not Link) for Return Home to force full reload
    - Neither file uses Framer Motion (not needed for error/404 pages)
  </acceptance_criteria>
  <done>404 page shows branded "Page Not Found" with orange 404 heading and Return Home button; error page shows "Something went wrong" with Try Again and Return Home buttons</done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. After `npx next dev`, visiting `/nonexistent-route` shows the branded 404 page with Header/Footer
3. `/sitemap.xml` returns valid XML with all routes
4. `/robots.txt` returns valid robots file
5. View page source of homepage -- `<meta property="og:image"` points to og-image.png
</verification>

<success_criteria>
- sitemap.xml accessible and lists 5 static routes + 19 project routes (24 total URLs)
- robots.txt accessible with `Allow: /` and sitemap reference
- OG image file exists at public/og-image.png
- Root layout metadata has metadataBase, title template, and openGraph configuration
- 404 page renders with orange-branded design and navigation home
- Error boundary renders recovery UI with retry and navigate-home options
</success_criteria>

<output>
After completion, create `.planning/phases/06-server-components-seo-error-pages/06-01-SUMMARY.md`
</output>
