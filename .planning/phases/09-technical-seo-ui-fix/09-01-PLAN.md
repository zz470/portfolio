---
phase: 09-technical-seo-ui-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/jsonld.ts
  - app/layout.tsx
  - app/page.tsx
  - app/portfolio/page.tsx
  - app/services/page.tsx
  - app/about/page.tsx
  - app/contact/page.tsx
  - components/navigation/footer.tsx
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-06
  - UI-01

must_haves:
  truths:
    - "Homepage HTML contains JSON-LD with Person and ProfessionalService schemas"
    - "Every page has a unique keyword-rich title visible in browser tab"
    - "Every page has a meta description referencing concrete credentials"
    - "Every page has twitter:card metadata"
    - "Footer social links have 44x44px minimum touch targets"
  artifacts:
    - path: "lib/jsonld.ts"
      provides: "JSON-LD schema builder functions"
      exports: ["buildPersonSchema", "buildLocalBusinessSchema", "buildCreativeWorkSchema"]
    - path: "app/layout.tsx"
      provides: "Root metadata with updated title, Twitter card defaults, JSON-LD script"
      contains: "application/ld+json"
    - path: "components/navigation/footer.tsx"
      provides: "Touch-target-compliant social links"
      contains: "min-h-[44px]"
  key_links:
    - from: "app/layout.tsx"
      to: "lib/jsonld.ts"
      via: "import buildPersonSchema, buildLocalBusinessSchema"
      pattern: "import.*from.*jsonld"
    - from: "app/layout.tsx"
      to: "lib/social-links.tsx"
      via: "JSON-LD sameAs uses social link URLs"
      pattern: "sameAs"
---

<objective>
Add JSON-LD structured data to the homepage, optimize all page titles and meta descriptions with keywords, add Twitter card metadata to all pages, and fix footer social link touch targets.

Purpose: Make the site fully machine-readable for search engines and social platforms, and fix a mobile UX defect.
Output: `lib/jsonld.ts` (new), updated metadata on all 6 page files, updated `app/layout.tsx` with JSON-LD and Twitter defaults, touch-target fix on footer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-technical-seo-ui-fix/09-CONTEXT.md
@.planning/phases/09-technical-seo-ui-fix/09-RESEARCH.md

@lib/copy.ts
@lib/social-links.tsx
@lib/contact-info.ts
@app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JSON-LD builder module and update root layout</name>
  <read_first>
    - lib/social-links.tsx
    - lib/contact-info.ts
    - lib/data/projects.ts (for Project type import)
    - app/layout.tsx
  </read_first>
  <files>lib/jsonld.ts, app/layout.tsx</files>
  <action>
1. Create `lib/jsonld.ts` with three exported functions:

   - `buildPersonSchema()` returns a Person JSON-LD object:
     - @context: "https://schema.org"
     - @type: "Person"
     - name: "Lorenzo Pardell"
     - url: "https://lorenzopardell.com"
     - jobTitle: "Re-Recording Mixer and Sound Designer"
     - description: "Re-recording mixer and sound designer with 30+ credits across film, series, and documentaries. Based in Sao Paulo, available worldwide."
     - email: contactInfo.email (import from @/lib/contact-info)
     - telephone: contactInfo.phone
     - address: { @type: "PostalAddress", addressLocality: "São Paulo", addressRegion: "SP", addressCountry: "BR" }
     - sameAs: socialLinks.filter(l => l.visible).map(l => l.href) (import socialLinks from @/lib/social-links)
     - knowsAbout: ["Sound Design", "Re-Recording Mix", "Dialogue Editing", "ADR Supervision", "Foley", "Dolby Atmos", "Film Audio Post-Production"]

   - `buildLocalBusinessSchema()` returns a ProfessionalService JSON-LD object:
     - @context: "https://schema.org"
     - @type: "ProfessionalService"
     - name: "Lorenzo Pardell"
     - url: "https://lorenzopardell.com"
     - telephone: contactInfo.phone
     - email: contactInfo.email
     - address: same PostalAddress as Person
     - areaServed: ["BR", "ES", "PT", "US"]
     - serviceType: ["Re-Recording Mix", "Sound Design", "Dialogue Editing", "Audio Post-Production"]
     - founder: { @type: "Person", name: "Lorenzo Pardell" }

   - `buildCreativeWorkSchema(project: Project)` returns a CreativeWork JSON-LD object (per D-02):
     - @context: "https://schema.org"
     - @type: "CreativeWork"
     - name: project.title
     - description: project.description
     - dateCreated: project.release_date?.toString()
     - productionCompany: conditionally { @type: "Organization", name: project.production_company } if present
     - contributor: { @type: "Role", contributor: { @type: "Person", name: "Lorenzo Pardell", url: "https://lorenzopardell.com" }, roleName: project.roles }
     - url: project.imdb_url (if present)
     - image: `https://lorenzopardell.com${project.hero_url}` (if hero_url present)

   Import `type Project` from `@/lib/data/projects`. Import `socialLinks` from `@/lib/social-links`. Import `contactInfo` from `@/lib/contact-info`.

2. Update `app/layout.tsx`:

   a. Import `buildPersonSchema` and `buildLocalBusinessSchema` from `@/lib/jsonld`.

   b. Update the metadata export:
      - Change title.default to: "Lorenzo Pardell | Sound Designer & Re-Recording Mixer | Sao Paulo" (per D-05)
      - Update description to: "Re-recording mixer and sound designer with 30+ credits across film, series, and documentaries. Based in Sao Paulo, Brazil. Available for remote and on-location projects worldwide."
      - Add twitter field (per D-04):
        ```
        twitter: {
          card: "summary",
          title: "Lorenzo Pardell | Sound Designer & Re-Recording Mixer | Sao Paulo",
          description: "Re-recording mixer and sound designer with 30+ credits across film, series, and documentaries. Based in Sao Paulo, Brazil.",
          images: ["/og-image.png"],
        }
        ```
      - Update openGraph title to match the new default title.
      - Update openGraph description to match the new description.

   c. Add two JSON-LD script tags inside the `<body>` element, BEFORE `<Header />`:
      ```tsx
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: JSON.stringify(buildPersonSchema()) }}
      />
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: JSON.stringify(buildLocalBusinessSchema()) }}
      />
      ```

   IMPORTANT: The root layout is a Server Component (no "use client"). JSON-LD scripts must go here, not in client components.
  </action>
  <verify>
    <automated>cd /Users/lorenzopardell/Desktop/DEV/NEXTJS/_PORTOFOLIO/next-portfolio && npx tsc --noEmit 2>&1 | head -20 && grep -c "application/ld+json" app/layout.tsx && grep -c "buildPersonSchema\|buildLocalBusinessSchema\|buildCreativeWorkSchema" lib/jsonld.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/jsonld.ts exports buildPersonSchema, buildLocalBusinessSchema, buildCreativeWorkSchema
    - app/layout.tsx contains two `<script type="application/ld+json">` elements
    - app/layout.tsx title.default includes "Sao Paulo" (per D-05)
    - app/layout.tsx has twitter.card = "summary" (per D-04)
    - TypeScript compiles with zero errors
  </acceptance_criteria>
  <done>JSON-LD builder module exists with 3 exported functions. Root layout renders Person + ProfessionalService JSON-LD, has updated default title with location, Twitter card defaults, and updated OG metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Optimize all page metadata and fix footer touch targets</name>
  <read_first>
    - app/page.tsx
    - app/portfolio/page.tsx
    - app/services/page.tsx
    - app/about/page.tsx
    - app/contact/page.tsx
    - lib/copy.ts (for meta description values)
    - components/navigation/footer.tsx
  </read_first>
  <files>app/page.tsx, app/portfolio/page.tsx, app/services/page.tsx, app/about/page.tsx, app/contact/page.tsx, components/navigation/footer.tsx</files>
  <action>
1. Update metadata on each page. CRITICAL: The root layout has `title.template: "%s | Lorenzo Pardell"`. Page-level `metadata.title` strings must NOT include "| Lorenzo Pardell" — the template appends it automatically. The copy.ts meta titles (aboutMeta.title, servicesMeta.title, contactMeta.title) all include "| Lorenzo Pardell" and MUST be overridden with inline strings, not used directly.

   **app/page.tsx** (homepage):
   - Keep no explicit title (root layout default applies)
   - Update openGraph.title to: "Lorenzo Pardell | Sound Designer & Re-Recording Mixer | Sao Paulo"
   - Update openGraph.description to: "Re-recording mixer and sound designer with 30+ credits across film, series, and documentaries. Based in Sao Paulo, Brazil."
   - Add twitter field: `twitter: { card: "summary", images: ["/og-image.png"] }`

   **app/portfolio/page.tsx**:
   - Change title to: "Sound Design Portfolio | Film, Series & Documentary" (per D-06, template appends "| Lorenzo Pardell")
   - Change description to: "Portfolio of 30+ sound design and re-recording mix credits across feature films, Netflix series, documentaries, and commercials. Based in Sao Paulo, Brazil."
   - Update openGraph.title to: "Sound Design Portfolio | Film, Series & Documentary | Lorenzo Pardell"
   - Update openGraph.description to match new description
   - Add twitter field: `twitter: { card: "summary", images: ["/og-image.png"] }`

   **app/services/page.tsx**:
   - Change title to: "Sound Design & Mixing Expertise" (per D-06 — NOT copy.servicesMeta.title which has suffix)
   - Keep description from copy.servicesMeta.description (it's good, no suffix issue)
   - Update openGraph.title to: "Sound Design & Mixing Expertise | Lorenzo Pardell"
   - Update openGraph.description to match
   - Add twitter field: `twitter: { card: "summary", images: ["/og-image.png"] }`

   **app/about/page.tsx**:
   - Change title to: "About | Re-Recording Mixer & Sound Designer" (per D-06 — NOT copy.aboutMeta.title which has suffix)
   - Keep description from copy.aboutMeta.description (it's good)
   - Update openGraph.title to: "About | Re-Recording Mixer & Sound Designer | Lorenzo Pardell"
   - Update openGraph.description to match
   - Add twitter field: `twitter: { card: "summary", images: ["/og-image.png"] }`

   **app/contact/page.tsx**:
   - Change title to: "Contact | Sound Designer Sao Paulo" (per D-06 — NOT copy.contactMeta.title which has suffix)
   - Keep description from copy.contactMeta.description (it's good)
   - Update openGraph.title to: "Contact | Sound Designer Sao Paulo | Lorenzo Pardell"
   - Update openGraph.description to match
   - Add twitter field: `twitter: { card: "summary", images: ["/og-image.png"] }`

2. Fix footer touch targets (per D-10, UI-01):
   In `components/navigation/footer.tsx`, on the social link `<Link>` element (line 84-89), add `min-h-[44px] min-w-[44px]` to the className. The existing classes are:
   `"flex items-center text-sm text-gray-600 hover:text-orange-500 transition-colors group"`
   Change to:
   `"flex items-center min-h-[44px] min-w-[44px] text-sm text-gray-600 hover:text-orange-500 transition-colors group"`
   Icons stay the same visual size. Only the tappable area increases.
  </action>
  <verify>
    <automated>cd /Users/lorenzopardell/Desktop/DEV/NEXTJS/_PORTOFOLIO/next-portfolio && npx tsc --noEmit 2>&1 | head -20 && grep -c "twitter" app/portfolio/page.tsx app/services/page.tsx app/about/page.tsx app/contact/page.tsx app/page.tsx && grep "min-h-\[44px\]" components/navigation/footer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/portfolio/page.tsx title contains "Sound Design Portfolio"
    - app/services/page.tsx title is "Sound Design & Mixing Expertise" (no "Lorenzo Pardell" suffix)
    - app/about/page.tsx title is "About | Re-Recording Mixer & Sound Designer" (no "Lorenzo Pardell" suffix)
    - app/contact/page.tsx title is "Contact | Sound Designer Sao Paulo" (no "Lorenzo Pardell" suffix)
    - All 5 page files contain a twitter metadata field
    - components/navigation/footer.tsx contains min-h-[44px] and min-w-[44px] on social link
    - TypeScript compiles with zero errors
  </acceptance_criteria>
  <done>All 5 pages have keyword-rich titles (no double-suffix), optimized descriptions, and Twitter card metadata. Footer social links meet 44x44px touch target minimum.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with zero TypeScript errors
- View source of built homepage contains `application/ld+json` script tags with Person and ProfessionalService
- Browser tab on each page shows keyword-rich title with " | Lorenzo Pardell" appended by template (no double suffix)
- Each page has `twitter:card` meta tag in HTML source
- Footer social links have 44x44px minimum tappable area (inspect in Chrome DevTools mobile view)
</verification>

<success_criteria>
- JSON-LD Person + ProfessionalService schemas render in homepage HTML
- All 5 pages have unique, keyword-rich titles per D-06
- All 5 pages have meta descriptions referencing credentials per D-08
- All pages have Twitter card metadata per D-04
- Footer social links are 44x44px minimum touch targets per D-10
- Zero TypeScript errors, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-technical-seo-ui-fix/09-01-SUMMARY.md`
</output>
